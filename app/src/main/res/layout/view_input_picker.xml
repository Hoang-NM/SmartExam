<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="hint"
            type="String" />

        <variable
            name="hintContent"
            type="String" />

        <variable
            name="icon"
            type="android.graphics.drawable.Drawable" />

        <variable
            name="content"
            type="String" />

        <variable
            name="onClickInput"
            type="android.view.View.OnClickListener" />
    </data>

    <com.google.android.material.textfield.TextInputLayout
        style="@style/AppWidget.TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@{hint}"
        app:endIconDrawable="@{icon}"
        app:endIconMode="custom"
        app:errorIconDrawable="@null"
        tools:endIconDrawable="@drawable/ic_calendar"
        tools:hint="Hint">

        <com.google.android.material.textfield.TextInputEditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:cursorVisible="false"
            android:focusable="false"
            android:hint="@{hintContent}"
            android:inputType="none"
            android:text="@{content}"
            android:textColorHint="?hintTextColor"
            app:onSafeClick="@{onClickInput}"
            tools:hint="@tools:sample/cities"
            tools:ignore="KeyboardInaccessibleWidget" />
    </com.google.android.material.textfield.TextInputLayout>
</layout>